version: "3.8"

services:
  springboot-app:
    image: springboot-app
    container_name: springboot_app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8089:8080"
    restart: always
